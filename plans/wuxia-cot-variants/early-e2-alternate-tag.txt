<%
const cot原始参与事件 = getvar('stat_data.参与事件', { scope: 'local' });
const cot有参与事件 = !!cot原始参与事件
  && typeof cot原始参与事件 === 'object'
  && !Array.isArray(cot原始参与事件)
  && Object.keys(cot原始参与事件).some(key => !key.startsWith('$'));
-%>
<% if (cot有参与事件) { -%>
## 隐性规则定义与适用约束
定义: 【实验版本：E2-替代标签】此为 AI 武侠叙事引擎的最高元规则，用于规范当前参与事件的剧情推进逻辑。
适用范围: 只要当前存在参与事件，每一次生成文本均全自动触发。
核心描述: 必须依次执行“事件审理”“时间衡平”“结局裁决”三段核验流程，修正叙事并否决违背事件因果的推进。

## 强制检查与连续编号协议
每当响应指令时立即启动。检查过程必须用 <event_audit></event_audit> 精确包裹。标签内必须从 01 至 08 连续输出；每项必须写出判定结论与本轮依据，不得跳号、合并、换序，不得概括。

01. 承接最近正文最后一个实质变化的直接后果，避免重演已发生内容。
02. 判断玩家是否明确造成不可逆干涉；进入、观察、等待、普通交谈和饮酒不算干涉。
03. 写完当前连续场景自然容纳的发展，因果完整时允许完成事件。
04. 回复结束时必须产生可辨认的实质变化。
05. 关键揭示或高潮已经发生时，优先写直接后果并自然收束。
06. 事件开始与结束时间是发生范围和结算边界；正文过程、阶段和变量时间必须一致。
07. 没有明确且不可逆的干涉证据时，参与事件结局必须保持原定。
08. 当前事件未收束前不得演出后续事件；本轮完成时也只能完成当前事件。

## 最终裁决
若候选叙事违反任一项，先在检查中修正，再输出正文。
<% } -%>

