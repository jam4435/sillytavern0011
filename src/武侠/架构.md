# 金庸群侠传前端架构入口

## 文档用途

本文件只保留稳定边界、关键入口和阅读顺序，不复述实现细节。

## 项目边界

金庸群侠传是运行在酒馆助手无沙盒 iframe 中的 React 武侠 RPG 前端。

- 聊天楼层保存 user/assistant 消息与 swipe。
- 聊天级 `stat_data` 保存当前游戏持久变量。
- React `GameState` 只是 UI 投影，不是持久源，也不是差分依据。
- 普通变量写入分两类：ERA 写入、酒馆助手变量函数直接写入。
- 消息边界事件负责刷新与兜底，不负责代替真实写入。
- 初始化链路独立于普通回合写入链路。

## 核心约定

### 1. `stat_data` 是唯一差分真相源

- 差分、归因、写入验证统一以 `getVariables({ type: 'chat' }).stat_data` 为准。
- `getAllVariables()` 只用于读取合并视图并投影到 `GameState`。
- 不要用 React 状态、事件 payload 或临时缓存判断“是否真的写入成功”。

### 2. 普通操作优先走 ERA

- AI 变量块、额外变量、功法普通操作、境界普通操作、后台补全，统一视为 ERA 写入链路。
- ERA 写入完成后等待匹配的 `era:writeDone`，再重读聊天级 `stat_data`。

### 3. 直接写入走变量函数 + 专用事件

- 变量编辑器或其他直接聊天变量修改，使用酒馆助手变量函数，例如 `updateVariablesWith()`。
- 直接写入完成后发 `wuxia:directVariableWriteDone`。
- 直接写入不得伪造 `era:writeDone`。

### 4. 消息边界只做兜底

- `MESSAGE_RECEIVED`、`MESSAGE_UPDATED`、`MESSAGE_SWIPED`、`CHAT_CHANGED` 用于刷新正文、选项和补偿重读。
- 当写入完成事件缺失、延迟或来自外部操作时，消息边界负责兜底重读 `stat_data`。

### 5. 初始化独立

- 新游戏初始化、开局建档、已有存档接管是独立链路。
- 初始化负责建立首份可用 `stat_data` 与界面初值，不与普通功法/境界操作混成一条规则。

## 数据源速查

| 数据         | 读取入口                                   | 用途                           |
| ------------ | ------------------------------------------ | ------------------------------ |
| 合并变量视图 | `getAllVariables()`                        | `readGameDataPure()` 投影 UI   |
| 聊天实际变量 | `getVariables({ type: 'chat' }).stat_data` | 差分、归因、写入验证、变量编辑 |
| 聊天楼层     | `getChatMessages()`                        | 正文、楼层 ID、swipe、边界兜底 |
| 角色级变量   | `getVariables({ type: 'character' })`      | checkpoint / 存档树            |
| 显示设置     | `localStorage`                             | 主题、外观、周边栏目/弹窗透明度、正则、额外模型和阈值 |
| 当前回合观测 | `sessionStorage`                           | 变量变更追踪的刷新恢复         |

## 关键入口

| 入口                                                                       | 职责                                                                |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| [`App.tsx`](./App.tsx)                                                     | 装配页面、消息、调试与变量追踪                                      |
| [`hooks/useMessageHandler.ts`](./hooks/useMessageHandler.ts)               | 手动发送、自动推进、重新生成                                        |
| [`hooks/useEventListeners.ts`](./hooks/useEventListeners.ts)               | 酒馆事件、写入完成事件、消息边界兜底                                |
| [`hooks/useVariableChangeTracker.ts`](./hooks/useVariableChangeTracker.ts) | 当前回合变量差分与归因                                              |
| [`utils/variableReader.ts`](./utils/variableReader.ts)                     | 纯读取、正文归一化、后台补全调度                                    |
| [`utils/frontendDerivedVariables.ts`](./utils/frontendDerivedVariables.ts) | 发送前同步聊天级 `前端变量`：周围地点、战力区、随机数、修为变化参考 |
| [`utils/locationContext.ts`](./utils/locationContext.ts)                   | 当前地图区域解析、相邻二级地点计算                                  |
| [`components/panels/MapPanel.tsx`](./components/panels/MapPanel.tsx)       | 地图面板装配、地点索引开关、地图数据加载                            |
| [`hooks/useCommandQueue.ts`](./hooks/useCommandQueue.ts)                   | 待发送指令队列、地图移动指令替换与附加发送                          |
| [`utils/variableEditorState.ts`](./utils/variableEditorState.ts)           | 直接聊天变量编辑与冲突检测                                          |
| [`utils/messageActions.ts`](./utils/messageActions.ts)                     | ERA 等待、swipe 回滚与重算                                          |
| [`components/SettingsPanel.tsx`](./components/SettingsPanel.tsx)           | 设置、变量页、调试页                                                |

## 按任务阅读

| 修改内容                                       | 必读文档                                         |
| ---------------------------------------------- | ------------------------------------------------ |
| 页面切换、开局、主界面状态、存档               | [01-页面与状态](./docs/01-页面与状态.md)         |
| 手动发送、选项、自动推进、重新生成             | [02-消息生命周期](./docs/02-消息生命周期.md)     |
| 变量读取、写入、变量编辑器                     | [03-变量读取与写入](./docs/03-变量读取与写入.md) |
| 变量条、AI/后台归因、差分                      | [04-变量变更追踪](./docs/04-变量变更追踪.md)     |
| 酒馆事件、补全、消息边界兜底                   | [06-事件与自动补全](./docs/06-事件与自动补全.md) |
| 地图面板、地点索引、地图指令队列、周围地点同步 | [09-地图与指令](./docs/09-地图与指令.md)         |
| 故障定位、日志、回归测试                       | [08-调试与排错](./docs/08-调试与排错.md)         |

## 稳定约束

1. 持久游戏状态必须落在聊天变量或聊天楼层，不能只改 React 状态。
2. `readGameDataPure()` 只读；补全写回单独调度。
3. 差分与归因只认聊天级 `stat_data`。
4. 普通功法/境界操作走 ERA；初始化独立。
5. 直接写入使用变量函数并发 `wuxia:directVariableWriteDone`，不得伪造 `era:writeDone`。
6. 消息边界负责兜底重读，不替代显式写入完成信号。

## 其他资料

- [`CHANGELOG.md`](./CHANGELOG.md)：历史问题和修复记录。
- [`功法提取模板.md`](./功法提取模板.md)：功法数据模板。
- [`修为境界功法属性战斗的体系架构.txt`](./修为境界功法属性战斗的体系架构.txt)：游戏规则体系。
